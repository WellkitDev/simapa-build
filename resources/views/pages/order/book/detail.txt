@extends('layouts.master')

@section('title', 'Detail Order - ' . $order->code_order . ' | SiMAPA')

@section('content')
<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3">Detail Order: <strong>{{ $order->code_order }}</strong></h1>
        <a href="{{ route('orders.index') }}" class="btn btn-secondary">
            ← Kembali ke Daftar
        </a>
    </div>

    <!-- Card Utama: Informasi Order -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Informasi Order</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <table class="table table-borderless">
                        <tr>
                            <th width="30%">Kode Order</th>
                            <td>: {{ $order->code_order }}</td>
                        </tr>
                        <tr>
                            <th>Jenis Layanan</th>
                            <td>:
                                @switch($order->type)
                                    @case('bk_mandiri') Buku Mandiri @break
                                    @case('bk_kolab') Buku Kolaborasi @break
                                    @case('at_mandiri') Artikel Jurnal Mandiri @break
                                    @case('at_kolab') Artikel Jurnal Kolaborasi @break
                                @endswitch
                                <span class="badge bg-info ms-2">
                                    {{ ucfirst($order->naskah_type) }}
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Judul</th>
                            <td>: {{ $order->title }}</td>
                        </tr>
                        @if($order->chapters)
                        <tr>
                            <th>Jumlah Bab</th>
                            <td>: {{ $order->chapters }} Bab</td>
                        </tr>
                        @endif
                        @if($order->scope)
                        <tr>
                            <th>Scope</th>
                            <td>: {{ $order->scope->scope }}</td>
                        </tr>
                        @endif
                        @if($order->indexation)
                        <tr>
                            <th>Target Indeksasi</th>
                            <td>: {{ $order->indexation }}</td>
                        </tr>
                        @endif
                        <tr>
                            <th>Jenis Publikasi</th>
                            <td>:
                                <span class="badge {{ $order->publication_type == 'fastrack' ? 'bg-danger' : 'bg-success' }}">
                                    {{ ucwords($order->publication_type) }}
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Status Order</th>
                            <td>:
                                <span class="badge {{ $order->status == 'completed' ? 'bg-success' : 'bg-warning' }}">
                                    {{ ucwords(str_replace('_', ' ', $order->status)) }}
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Marketing</th>
                            <td>: {{ $order->marketing->name }}</td>
                        </tr>
                    </table>
                </div>

                <div class="col-md-6">
                    <h5 class="mb-3">Penulis</h5>
                    <ol class="list-group list-group-numbered">
                        @foreach($order->authors->sortBy('pivot.possition') as $author)
                            <li class="list-group-item d-flex justify-content-between align-items-start">
                                <div class="ms-2 me-auto">
                                    <div class="fw-bold">{{ $author->name }}</div>
                                    @if($author->affiliation)
                                        <small class="text-muted">{{ $author->affiliation }}</small><br>
                                    @endif
                                    @if($author->email || $author->phone)
                                        <small class="text-muted">
                                            {{ $author->email ?? '' }}
                                            {{ $author->email && $author->phone ? ' | ' : '' }}
                                            {{ $author->phone ?? '' }}
                                        </small>
                                    @endif
                                </div>
                                <span class="badge bg-primary rounded-pill">Posisi {{ $author->pivot->possition }}</span>
                            </li>
                        @endforeach
                    </ol>
                </div>
            </div>

            @if($order->note)
            <div class="mt-4">
                <h5>Catatan Tambahan</h5>
                <div class="alert alert-info">
                    {{ $order->note }}
                </div>
            </div>
            @endif
        </div>
    </div>

    <!-- Card: Rincian Keuangan -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0">Rincian Keuangan</h5>
        </div>
        <div class="card-body">
            <div class="row text-center mb-4">
                <div class="col">
                    <h4>Rp {{ number_format($order->cost_amount, 0, ',', '.') }}</h4>
                    <small class="text-muted">Total Biaya</small>
                </div>
                <div class="col">
                    <h4>Rp {{ number_format($order->pay_amount, 0, ',', '.') }}</h4>
                    <small class="text-muted">Sudah Dibayar</small>
                </div>
                <div class="col">
                    <h4 class="{{ $order->debit_amount <= 0 ? 'text-success' : 'text-danger' }}">
                        Rp {{ number_format($order->debit_amount, 0, ',', '.') }}
                    </h4>
                    <small class="text-muted">Sisa Tagihan</small>
                </div>
            </div>

            <h5>Riwayat Pembayaran</h5>
            @if($order->payments->count() > 0)
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Tanggal</th>
                        <th>Jenis</th>
                        <th>Jumlah</th>
                        <th>Bukti</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($order->payments as $i => $payment)
                    <tr>
                        <td>{{ $i + 1 }}</td>
                        <td>{{ $payment->date->format('d M Y H:i') }}</td>
                        <td>{{ ucwords($payment->type) }}</td>
                        <td>Rp {{ number_format($payment->amount, 0, ',', '.') }}</td>
                        <td>
                            @if($payment->struk_url)
                                <a href="{{ asset('storage/' . $payment->struk_url) }}" target="_blank" class="btn btn-sm btn-outline-primary">
                                    Lihat Bukti
                                </a>
                            @endif
                        </td>
                        <td>
                            <span class="badge bg-success">Terbayar</span>
                        </td>
                    </tr>
                    @endforeach
                </tbody>
            </table>
            @else
                <p class="text-muted">Belum ada pembayaran tercatat.</p>
            @endif
        </div>
    </div>

    <!-- Card: Invoice -->
    <div class="card shadow-sm">
        <div class="card-header bg-info text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Invoice</h5>
            @if($order->invoice && $order->invoice->inv_pdf_url)
                <a href="{{ asset($order->invoice->inv_pdf_url) }}" target="_blank" class="btn btn-light btn-sm">
                    📄 Download PDF Invoice
                </a>
            @endif
        </div>
        <div class="card-body text-center">
            @if($order->invoice)
                <p><strong>No. Invoice:</strong> {{ $order->invoice->inv_no }}</p>
                <p><strong>Tanggal Terbit:</strong> {{ $order->invoice->issued_at->format('d F Y') }}</p>
                @if($order->invoice->dued_at)
                    <p><strong>Jatuh Tempo:</strong> {{ $order->invoice->dued_at->format('d F Y') }}</p>
                @endif

                <h4 class="mt-4">
                    Status:
                    <span class="{{ $order->debit_amount <= 0 ? 'text-success' : 'text-warning' }} fw-bold">
                        {{ $order->debit_amount <= 0 ? 'LUNAS' : 'MENUNGGU PELUNASAN' }}
                    </span>
                </h4>
            @else
                <p class="text-muted">Invoice belum digenerate.</p>
                <form action="{{ route('orders.generateInvoice', $order->id) }}" method="POST" class="d-inline">
                    @csrf
                    <button type="submit" class="btn btn-outline-primary">
                        Generate Invoice Sekarang
                    </button>
                </form>
            @endif
        </div>
    </div>
</div>
@endsection
